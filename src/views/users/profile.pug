extends ../base
include ../mixins/video

block content
  div.profile__data
    if loggedInUser.avatarUrl === ""
      span.avatar 😀
    else
      if loggedInUser.avatarUrl.includes("github")
        img(src=loggedInUser.avatarUrl, crossorigin).avatar
      else
        img(src="/" + loggedInUser.avatarUrl, crossorigin).avatar

    a(href="/users/edit").edit-btn
      i.fas.fa-pen    
        
    h3=user.name
  div.video-grid
    each video in user.videos
      +video(video)
    else
      span.empty__message No videos found